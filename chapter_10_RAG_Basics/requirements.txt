# Chapter 10 — PRD RAG Explorer · Python dependencies
#
# THIS FILE IS WHAT VERCEL INSTALLS, so it deliberately contains only what the deployed function
# needs at runtime: the web framework, PDF parsing, HTTP clients, and the remote vector store.
#
# The local development extras (ChromaDB for the embedded vector store, uvicorn as the dev server)
# live in requirements-local.txt, which includes this file.
#
# Keep this list lean — Python functions are not tree-shaken and the bundle limit is 500 MB.

fastapi>=0.115.0
pypdf>=5.0.0
groq>=0.11.0
httpx>=0.27.0
numpy>=1.26.0
python-dotenv>=1.0.1
python-multipart>=0.0.9      # multipart parsing for the /api/ingest file-upload path
qdrant-client>=1.12.0        # imported lazily — only loaded when VECTOR_STORE=qdrant
