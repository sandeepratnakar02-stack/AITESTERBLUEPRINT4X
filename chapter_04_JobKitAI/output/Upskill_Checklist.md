# Skills Upskill Checklist — based on GAP analysis (26 Aug 2026)

These are the skills each role wanted that your resume could **not** honestly claim.
Use this as a prioritized learning plan. Learn → build a small proof-of-concept →
add the tool to your resume only once you can genuinely claim it.

---

## 1. SayaCare (Noida) — QA Automation Engineer

| Skill | Priority | Why / next step |
|-------|----------|-----------------|
| **Playwright** | HIGH | Replaces/extends Selenium; write a small test suite against a public site in Playwright (Node or Python). |
| **JavaScript / Node.js** | HIGH | Their stack is React/Next.js backend; learn JS basics + write API tests in Node + a couple of Playwright scripts. |
| **Python** | MEDIUM | Increasingly required for API/backend QA; do the same API tests in Python (pytest + requests). |
| **PostgreSQL** | MEDIUM | You know SQL/MySQL; run the same queries on Postgres to claim it credibly. |
| **Mobile/Android (Flutter/Dart)** | LOW | Only worth it if you want mobile QA; start with Appium + a Flutter demo app. |
| **Firebase** | LOW | Optional backend knowledge; note it only if you actually use it. |

## 2. NVIDIA (Pune) — Software QA Tools Development Engineer

| Skill | Priority | Why / next step |
|-------|----------|-----------------|
| **Python** | HIGH | Their primary automation language; port one of your Ruby framework components to Python to prove it. |
| **C# / .NET Core** | HIGH | You already test .NET apps — now write automation/tooling in C# with .NET Core (xUnit + a small tool). |
| **GitHub (vs plain GIT)** | MEDIUM | You use Git; learn GitHub Actions + PR/CI workflows to claim GitHub specifically. |
| **AI/ML** | LOW | Huge topic; only mention if you build or test something ML-related. |
| **Perforce** | LOW | Version control for large binaries; optional, niche. |
| **Android** | LOW | Not core to this role; skip unless mobile QA interests you. |
| **Hardware/GPU certification QA** | LOW | Domain-specific; skip unless you get exposure. |

## 3. LOGZ (Chennai) — QA Automation Engineer

| Skill | Priority | Why / next step |
|-------|----------|-----------------|
| **Playwright** | HIGH | Same as SayaCare — strongest ROI skill for modern web QA. |
| **GitHub Actions** | MEDIUM | You own Jenkins pipelines; port one pipeline to GitHub Actions to claim it. |
| **Cypress** | MEDIUM | Popular JS E2E tool; a small demo suite is enough to claim familiarity. |
| **Appium** | LOW | Only if you want mobile automation on your resume. |
| **Azure DevOps** | LOW | You know Jenkins/GOCD; a quick hands-on with Azure Pipelines makes this honest to claim. |

## 3. Enhance_Resume_Score (Nagpur) — QA Automation Engineer

| Skill | Priority | Why / next step |
|-------|----------|-----------------|
| *(none)* | — | Near-complete match — no upskilling needed to apply. |

---

## Suggested learning order (if you want all four)

1. **Playwright + JavaScript/Node** — unlocks SayaCare + LOGZ.
2. **Python (pytest + requests)** — unlocks NVIDIA + strengthens SayaCare.
3. **GitHub Actions** — port one Jenkins pipeline → unlocks LOGZ + NVIDIA "GitHub".
4. **PostgreSQL** — quick SQL port → unlocks SayaCare.
5. **C#/.NET Core** — only if pursuing NVIDIA seriously.

Rule: only add a tool to your resume **after** you've written real code with it — never just list it.
